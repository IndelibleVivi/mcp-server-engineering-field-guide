# Synthetic MCP App

- Declared MCP revision: `2026-07-28`.
- Integration guidance bundled with the project was assessed on `2026-08-15`.
- This evaluation is offline and includes no current host documentation, browser execution, or named-host acceptance receipt.
- The tool returns model-visible content, component-visible structured data, and UI-only metadata.
- The widget uses a host-specific bridge when present and also receives `postMessage` events.
