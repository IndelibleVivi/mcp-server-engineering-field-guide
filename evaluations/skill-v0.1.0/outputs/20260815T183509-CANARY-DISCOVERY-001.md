# Projected final answer — `CANARY-DISCOVERY-001`

> Same-owner projected artifact. Temporary local paths were normalized to the checked-in synthetic fixture. The Git revision cited by the answer is the run-local materialization identity; `scenarios.json` content hashes are the public fixture authority. Private raw JSONL and tool traces are omitted.

结论：该实现静态上**不符合其声明的 MCP `2026-07-28` revision**。

### Applicable layers

- **Normative MCP layer:** `2026-07-28` specification。
- **Message substrate:** JSON-RPC 2.0；MCP 对 batching、message direction、result shape 等进一步收窄。
- **Transport layer:** parent-owned stdio，newline-delimited JSON；边界是 host → child `stdin/stdout`。
- **不适用:** HTTP RFC 9110/9112、Streamable HTTP routing headers、Host、Origin、CORS、TLS、proxy/tunnel、network authentication，以及 MCP Apps/host integration guidance。

### Findings

1. **缺少 2026-07-28 必需机制。**
   [server.py](../fixtures/parent-owned-stdio/server.py#L12) 不验证每个 request 的 protocol/client `_meta`，未实现必需的 `server/discover`，result 也缺少 `resultType` 和 server identity `_meta`。

2. **JSON-RPC classification 不合规。**
   未验证 `jsonrpc`、`id` presence/type、`method`、`params` 或 request/notification/response distinction。`tools/call` notification 会错误返回 `id: null`；未知 method 被静默丢弃；无效 tool/params 和 JSON parse failure 会抛异常，而不是产生规范 JSON-RPC error。

3. **`tools/call` result shape 不完整。**
   [server.py:22](../fixtures/parent-owned-stdio/server.py#L22) 返回裸字符串或 `null`，不是 revision 所要求的完整 MCP result envelope。

4. **Transport residual。**
   framing 与 [PROJECT.md:4](../fixtures/parent-owned-stdio/PROJECT.md#L4) 一致，但源码没有 input-line size/time budget；Python stdio/JSON parser 是实际 framing 和 parsing owner。

### Evidence ceiling

审计固定于 Git `64347dc38c20b7dcf3ba2e9509fffe70734bdff8`，仅检查 source 和 documentation。因此最高只能说“源码看起来实现/未实现某行为”。parent host launch、实际 framing、process survival、host discovery/invocation 和 compatibility 均为**未验证**；没有 runtime、integration、host acceptance 或 independent reproduction evidence。

未运行 server，未修改文件；worktree 保持 clean。
