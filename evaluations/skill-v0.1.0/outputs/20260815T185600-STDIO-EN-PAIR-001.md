# Projected final answer — `STDIO-EN-PAIR-001`

> Same-owner projected artifact. Temporary local paths were normalized to the checked-in synthetic fixture. The Git revision cited by the answer is the run-local materialization identity; `scenarios.json` content hashes are the public fixture authority. Private raw JSONL and tool traces are omitted.

## Audit result

At Git revision `04fd0e7e53481f683bf15bc2e8f783ceefb201b0` on `master`, this repository does **not** support a claim of MCP `2026-07-28` conformance. Its only supportable evidence level is static source inspection.

### Applicable profiles

- `json-rpc-2.0` — applicable as the message substrate.
- `mcp-2026-07-28` — applicable because [PROJECT.md](../fixtures/parent-owned-stdio/PROJECT.md#L3) declares that revision.
- `core-invariants` — applicable, using the parent-owned stdio branch.
- `evidence-discipline` — applicable to bound the conclusions.

Not applicable:

- Earlier MCP revision profiles.
- HTTP RFC 9110/9112 and low-level HTTP profiles.
- MCP Apps and dated host-integration profiles.
- Streamable HTTP, HTTP routing headers, OAuth, proxy, tunnel, or browser controls.

### Findings

1. **Protocol-blocking — the declared 2026-07-28 envelope is absent.**
   The sole dispatcher recognizes only `tools/call` ([server.py](../fixtures/parent-owned-stdio/server.py#L12)). It has no `server/discover`, no per-request `_meta` revision/client-capability validation, no server identity in result `_meta`, and no `resultType`. Therefore, the executable source does not implement the declared revision’s required request and result shape.

2. **JSON-RPC 2.0 classification and errors are incomplete.**
   The server does not validate `jsonrpc`, distinguish requests from notifications or client responses, reject Boolean IDs, or return structured parse/invalid-request/method/params errors. A `tools/call` notification is executed and receives a response with `"id": null`; unknown methods are silently ignored. Malformed JSON, batch input, unknown tools, and invalid parameters appear capable of escaping as uncaught Python exceptions and terminating the process.

3. **Applicable input/output budgets are absent.**
   Input is consumed as an unbounded line before `json.loads` ([server.py](../fixtures/parent-owned-stdio/server.py#L25)). There is no line-size, parse-time, request-rate, output-size, write-backpressure, or cancellation control. The sequential loop effectively limits capability concurrency to one, but it does not bound incomplete or oversized input.

4. **The capability itself appears narrowly read-only.**
   `notes.lookup` validates a non-empty string key and reads a fixed in-memory mapping ([server.py](../fixtures/parent-owned-stdio/server.py#L9)). Static inspection shows no durable write, network access, or subprocess effect. Unknown additional argument fields are not rejected, and no discoverable argument schema is provided.

### Control applicability and ownership

| Control family | Status | Owner / observation |
|---|---|---|
| Executable selection, environment delivery, process admission, pipe reachability | Applicable, parent-owned | The local desktop host launches the child and controls access to its stdin/stdout, as declared in [PROJECT.md](../fixtures/parent-owned-stdio/PROJECT.md#L5). |
| Caller authority | Applicable, parent-owned at reachability boundary | Any caller able to write to the child’s stdin can reach `notes.lookup`. The server performs no caller identity or scope authorization. |
| MCP revision metadata and client identity | Applicable, server-owned | Required per request by the selected profile; not validated here. Client identity metadata is not itself network authentication. |
| Stdio framing, JSON decoding, JSON-RPC classification, dispatch, arguments, serialization | Applicable, server/runtime-owned | Line framing and JSON decoding exist; the remaining enforcement is incomplete. |
| Capability state locking and duplicate-effect identity | `not applicable` to this capability | The inspected capability has no mutable or durable effect and executes serially. |
| Host/effective authority, Origin, CORS, request-target, HTTP body framing, `Expect: 100-continue`, HTTP socket admission | `not applicable` | No HTTP wrapper or listener exists. |
| `Mcp-Method` / `Mcp-Name`, POST/GET/SSE, HTTP sessions and resumability | `not applicable` | Streamable HTTP is outside the declared transport. |
| TCP bind, TLS, proxy/tunnel trust, network authentication/OAuth | `not applicable` | No network path is declared or implemented. The parent process is a reachability and launch authority, **not** a network authentication protocol. |
| MCP Apps DOM, `postMessage`, CSP, widget authority | `not applicable` | No browser, UI resource, or component is present. |

### Evidence ceiling

The highest repository-only claim is:

> At source-inspection level, the code appears to implement a serial, parent-reachable, read-only lookup over a fixed in-memory mapping, but it does not implement the required MCP `2026-07-28` or complete JSON-RPC envelope.

No process behavior, host acceptance, deployment security, protocol compliance, or independent reproduction is supported. The repository explicitly contains no runtime or independent receipt ([PROJECT.md](../fixtures/parent-owned-stdio/PROJECT.md#L8)). I performed static inspection and Git identity/status checks only; the server was not executed, no external disclosure occurred, and no files were modified.
