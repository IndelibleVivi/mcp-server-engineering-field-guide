# Projected final answer — `HTTP-HIST-EN-001`

> Same-owner projected artifact. Temporary local paths were normalized to the checked-in synthetic fixture. The Git revision cited by the answer is the run-local materialization identity; `scenarios.json` content hashes are the public fixture authority. Private raw JSONL and tool traces are omitted.

## Audit result

At pinned source `a737e75cfb97dd0299387bc548c1e02775406525` on `master`, the declared profile is MCP `2025-06-18` over Streamable HTTP. I found three revision-independent defects. The historical session/SSE design itself is valid for that revision.

### Findings

1. **High — `tools/call` bypasses capability validation**

   [server.py:28](../fixtures/historical-http-2025-06-18/server.py#L28) passes `arguments["thought"]` directly to the effect, while `/think` uses `validate_thought`. This contradicts the documented shared contract in [PROJECT.md:7](../fixtures/historical-http-2025-06-18/PROJECT.md#L7).

   Reproduced function-level behavior:

   - blank input: REST rejects; MCP accepts and captures;
   - 2,001-character input: REST rejects; MCP accepts and captures;
   - list input: REST rejects; MCP accepts, captures, and emits a non-string `TextContent.text`.

   This bypasses argument and resource limits, permits invalid result content, and reaches the mutation before rejection. MCP `2025-06-18` requires tool inputs to be validated and defines `TextContent.text` as a string. [Official tools specification](https://modelcontextprotocol.io/specification/2025-06-18/server/tools)

   **Evidence:** reproduced, Python function boundary.
   **Residual:** actual HTTP reachability and upstream body limits are unknown.

2. **Medium — false `readOnlyHint`**

   [server.py:11](../fixtures/historical-http-2025-06-18/server.py#L11) advertises `readOnlyHint: true`, but capture is enabled by default and [server.py:33](../fixtures/historical-http-2025-06-18/server.py#L33) appends to mutable operator-visible state. The probe observed three invalid MCP inputs adding three entries.

   In this revision, `readOnlyHint: true` means the tool does not modify its environment. Although annotations are explicitly untrusted hints, the server metadata is still inaccurate. [Official 2025-06-18 schema](https://modelcontextprotocol.io/specification/2025-06-18/schema)

   **Evidence:** source inspection plus reproduced mutation.
   **Residual:** the real capture-log backend and persistence semantics are absent.

3. **Medium — declared tool descriptor lacks required `inputSchema`**

   `TOOL` contains only `name`, `description`, and `annotations` at [server.py:8](../fixtures/historical-http-2025-06-18/server.py#L8). The MCP `2025-06-18` `Tool` schema requires `inputSchema`.

   **Evidence:** verified code-level schema mismatch.
   **Residual:** no `tools/list` implementation is present, so whether this object is actually emitted on the wire is unknown.

### Historically valid behavior

`Mcp-Session-Id` and GET-based SSE are not defects under MCP `2025-06-18`. That revision permits:

- assigning `Mcp-Session-Id` in the initialization response;
- subsequent session-bound requests;
- GET on the MCP endpoint to establish an SSE stream.

These were later changed or removed, but later protocol rules must not be applied retroactively. [Official Streamable HTTP specification](https://modelcontextprotocol.io/specification/2025-06-18/basic/transports)

However, [historical_session_flow](../fixtures/historical-http-2025-06-18/server.py#L39) only formats a string. It does not prove session issuance, opacity, validation, expiry, termination, SSE framing, or routing. Therefore the lifecycle shape is **historically valid**, while this implementation’s compliance is **unknown**, not verified.

### Evidence boundary and residual unknowns

Evidence reached static inspection and reproduced Python function execution on Python `3.13.3`, Darwin `25.5.0 arm64`. No HTTP handler, listener, framework configuration, or deployment artifacts exist; [PROJECT.md:8](../fixtures/historical-http-2025-06-18/PROJECT.md#L8) acknowledges this.

Consequently, all wire-level behavior remains unknown: initialization ordering, `MCP-Protocol-Version`, session lifecycle, Origin/Host checks, authentication, content negotiation, JSON-RPC classification, batch rejection, `202 Accepted` handling, framing and body limits, timeouts, connection admission, SSE correctness, cancellation, retries, concurrency, TLS, and proxy behavior.

No files were modified; the worktree remained clean. No patch, deployment, or external disclosure was performed.
