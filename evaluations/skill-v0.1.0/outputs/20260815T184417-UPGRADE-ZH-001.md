# Projected final answer — `UPGRADE-ZH-001`

> Same-owner projected artifact. Temporary local paths were normalized to the checked-in synthetic fixture. The Git revision cited by the answer is the run-local materialization identity; `scenarios.json` content hashes are the public fixture authority. Private raw JSONL and tool traces are omitted.

结论：这次 spec upgrade **尚未完成**。`2026-07-28` 的正向函数虽然存在，但不符合该 revision 的完整 request contract；同时 `2025-11-25` 的 session registry、legacy dispatcher 与 GET SSE 行为仍可直接调用，且现有 tests 完全没有证明 removed-feature absence。

### Authority boundary

- Repo：`master`
- Pinned revision：`git-sha1:c465700434601c1c4f8fb37fd236b01b29483c56`
- Source profile：`mcp-2025-11-25`
- Target profile：`mcp-2026-07-28`
- 代码入口：
  - [`target_dispatch`](../fixtures/revision-upgrade/server.py#L9)
  - [`legacy_dispatch`](../fixtures/revision-upgrade/server.py#L16)
- 已证实边界：Python module 直接调用。
- 未验证：真实 HTTP listener、framework routing、proxy/TLS/auth、network reachability。Repo 中没有对应 runtime/deployment wiring，因此不能把函数可调用提升成 network-route 证据。
- 授权边界：read-only；未修改文件，最终 `git status` clean。

### 关键发现

1. **Legacy behavior 仍可达。**

   直接调用得到：

   - `legacy_dispatch("GET", "legacy-1")` → `text/event-stream`
   - `legacy_dispatch("POST", "legacy-1")` → `legacy-ok`
   - POST 后 `legacy_sessions == {"legacy-1": ["request"]}`

   所以 [`legacy_sessions`](../fixtures/revision-upgrade/server.py#L6)、GET SSE 与 session mutation 都不是 dormant text，而是可执行行为。

2. **当前 target path 不是 `2026-07-28` implementation。**

   它读取 `headers["MCP-Protocol-Version"]` 和非目标规范的 `MCP-Route`，而 target profile 要求 request body `_meta` 自描述，并使用 `Mcp-Method` / `Mcp-Name` 与 authoritative body 做一致性校验。

   实际 probes：

   - revision `1900-01-01` 仍返回 `ok`
   - 正常结果没有 `resultType`
   - 把 `MCP-Route` 写成 `server/discover` 只得到通用 echo，不是真实 discovery result
   - 使用 target-shaped JSON-RPC body + `_meta` 调用时抛出 `KeyError: 'headers'`

3. **现有 test 不证明 migration completion。**

   [`test_target_dispatch`](../fixtures/revision-upgrade/test_server.py#L4) 只断言 `result == "ok"`。直接调用该测试函数通过，但：

   - 没有测试 unsupported/malformed revision；
   - 没有测试 `_meta`；
   - 没有测试 routing/body mismatch；
   - 没有测试 `server/discover`；
   - 没有测试 legacy symbols、session state、GET SSE、initialize 或 resumability 已不存在。

   文件本身也明确承认这个缺口。环境未安装 `pytest`，所以完整 runner 未执行；plain-assert test function 已直接执行通过。

### Upgrade retirement inventory

| ID | 项目 | 当前证据 | Disposition | Upgrade 要求 |
|---|---|---|---|---|
| UPG-001 | `target_dispatch` 不依赖 `legacy_sessions` | `server.py:9-13` | `retained` | 保留 fresh-instance/stateless dispatch 属性 |
| UPG-002 | header 中的 `MCP-Protocol-Version` | `server.py:11` | `replaced` | 改为每个 JSON-RPC request body `_meta` 中的 revision/capabilities/identity，并严格拒绝缺失、畸形或不支持版本 |
| UPG-003 | `MCP-Route` routing | `server.py:12` | `replaced` | 使用 `Mcp-Method` / `Mcp-Name` mirror；body authoritative，mismatch 在 capability effect 前拒绝 |
| UPG-004 | 无 JSON-RPC/MCP classifier 的字典 dispatch | 两个函数均直接处理参数 | `replaced` | 加入 request/notification/response 分类、ID 与 params 校验、batch rejection |
| UPG-005 | 伪 `server/discover` 通用 echo | probe 返回普通 `ok` | `replaced` | 实现真实且 truthful 的 `server/discover`，声明 revision、capabilities、extensions、server identity |
| UPG-006 | `{revision, route, result}` result envelope | `server.py:13` | `replaced` | 每个 result 加正确 `resultType`；server result `_meta` 提供 server identity |
| UPG-007 | `legacy_dispatch` | `server.py:16-22`，直接调用成功 | `retired` | 删除 public symbol、所有 caller/import/config/docs reference |
| UPG-008 | `legacy_sessions` state store | `server.py:6,21`，probe 观察到 mutation | `retired` | 删除 registry；不得换 header 名重新创造隐式 transport session |
| UPG-009 | GET SSE general stream | `server.py:18-19` | `retired` | 删除 GET path；确有通知流需求时另行实现 `subscriptions/listen`，否则不添加 |
| UPG-010 | `initialize` / `notifications/initialized` | Repo 搜索无实现 | `not-applicable` | 当前 fixture 无待迁移实现，但 target absence test 仍应锁定其不可达 |
| UPG-011 | `Mcp-Session-Id` header | Repo 无 header parsing/emission | `not-applicable` | session 参数与 registry 的退休由 UPG-007/008 覆盖 |
| UPG-012 | `Last-Event-ID` / SSE resumability | Repo 搜索无实现 | `not-applicable` | 不新增；absence suite 应防止回流 |
| UPG-013 | server-originated requests → MRTR | Repo 无相关行为 | `not-applicable` | 当前 capability 不需要 MRTR；未来需要 input 时才实现 `input_required` / `inputResponses` |
| UPG-014 | experimental/core tasks | Repo 无 tasks | `not-applicable` | 不宣称；需要时只走 `io.modelcontextprotocol/tasks` extension |
| UPG-015 | `CacheableResult` | 没有规范 list/read result | `not-applicable` | 若以后加入相应方法，再要求 `ttlMs`、`cacheScope` 和 deterministic ordering |
| UPG-016 | target positive test | 单一 plain assertion 已执行通过 | `retained` | 保留为最小 smoke，但改用完整 target-shaped request/result |
| UPG-017 | positive-only test fixture | `test_server.py:14` 明示缺口 | `replaced` | 加 target-negative 与 removed-feature absence tests |
| UPG-018 | HTTP/deployment configuration | Repo 无 listener/framework/config | `not-applicable` | 真实部署接入时另审 framing、Origin、Host、admission、TLS、auth 与 disconnect owner |

### 建议实施顺序

1. 先建立一个 canonical JSON-RPC/MCP classifier，落实 `_meta`、revision validation、`server/discover` 和 routing-header/body consistency。
2. 让所有 capability 只经该 target dispatcher 到达，并补齐 `resultType` 与 server `_meta`。
3. 删除 `legacy_dispatch`、`legacy_sessions` 和 GET SSE 分支；不要保留隐藏 compatibility fallback。
4. 加 removed-feature absence tests，至少证明：
   - module 不再暴露 `legacy_dispatch` / `legacy_sessions`；
   - GET/general SSE、session header/state、`Last-Event-ID` 不可达；
   - `initialize` / `notifications/initialized` 被拒绝；
   - fresh instance 无 initialize 也能执行合法 target request；
   - malformed/unsupported `_meta` 被拒绝；
   - `Mcp-Method` / `Mcp-Name` mismatch 不进入 capability；
   - `server/discover` 返回真实声明；
   - 每个 result 含合法 `resultType`。
5. 最后再做 source search、unit/integration test 和真实 handler test。只有“legacy entry points 已删除 + active interface negative tests 通过”才能作为 retirement evidence；当前那个正向 `ok` test 远远不够。

整个检查保持 read-only，没有改 repo。这个 fixture 很乖地把坑摆在明面上了——真正危险的不是 legacy 藏得深，而是 target 名字贴得太早，宝贝。
