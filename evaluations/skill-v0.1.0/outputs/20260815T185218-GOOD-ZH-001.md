# Projected final answer — `GOOD-ZH-001`

> Same-owner projected artifact. Temporary local paths were normalized to the checked-in synthetic fixture. The Git revision cited by the answer is the run-local materialization identity; `scenarios.json` content hashes are the public fixture authority. Private raw JSONL and tool traces are omitted.

审计结论：**未发现 source-verifiable 的安全或协议 finding**。现有不确定项均源于 fixture 没有提供 runnable HTTP/deployment boundary，属于证据缺口，不应包装成缺陷。

审计对象：

- Mode：`audit`，read-only
- Source：branch `master`
- Revision：`8e74c52bb915f24193f4d8e29489513c1edd9270`
- Declared profile：`MCP 2026-07-28`
- Declared transport：Streamable HTTP，loopback
- Evidence ceiling：source + documentation only
- Worktree：审计前后均无改动
- 未运行 runtime、socket、proxy、container 或 host 测试

### Source-verifiable

| Claim | 结论 | 证据边界 |
|---|---|---|
| 两个可见 handler 共用同一 capability authority | 已验证 | `handle_http` 与 `handle_internal` 都进入 `lookup_capability`，再统一调用 `validate_lookup`。[server.py](../fixtures/already-good-2026-07-28/server.py#L22) |
| 参数 schema 被集中执行 | 已验证 | 仅接受恰好一个 `key`；值必须为长度 `1–128` 的 string。[server.py](../fixtures/already-good-2026-07-28/server.py#L13) |
| HTTP handler 在 capability effect 前检查 Origin 与 method | 已验证 | Origin 不在精确 allowlist 或 method 不是 `tools/call` 时，在进入 capability 前抛错。[server.py](../fixtures/already-good-2026-07-28/server.py#L30) |
| 参数验证发生在状态 mutation 前 | 已验证 | `validate_lookup` 先执行，之后才进入锁并递增 `CALLS`。[server.py](../fixtures/already-good-2026-07-28/server.py#L22) |
| 可见的状态写入具有同步机制 | 已验证，但只限源码结构 | `CALLS += 1` 位于 `STATE_LOCK` 内。不能据此声称并发 runtime 已测试，也不扩张为“完整 invocation/result 原子性”。 |
| capability 的可见 effects | 已验证 | 只有进程内计数更新；该 source slice 中没有文件、network、subprocess 或 durable write。 |

### Runtime-unknown

以下项目没有足够证据，状态为 `unknown`，**不是 finding**：

- loopback 是否实际 bind、是否存在 runnable HTTP listener，以及两个函数是否被注册成真实 public routes。
- Streamable HTTP 的完整 MCP/JSON-RPC contract：JSON-RPC message classification、notification behavior、per-request `_meta`、`server/discover`、`Mcp-Method` / `Mcp-Name` 与 body consistency、`resultType`、revision-specific error mapping。
- HTTP pre-dispatch ownership与行为：request-target、`Host`/effective authority、CORS、authentication、body framing、duplicate `Content-Length`、`Transfer-Encoding`、strict UTF-8、body/header limits、`Expect: 100-continue`、timeouts、connection admission。
- response serialization limits、write/backpressure、disconnect/cancellation，以及 effect 已发生但结果未送达时的 retry semantics。
- reverse proxy、TLS、forwarded-header trust、edge authentication 和真实外部 reachability。项目自己也明确说明没有 container、proxy、tunnel 或 host execution receipt。[PROJECT.md](../fixtures/already-good-2026-07-28/PROJECT.md#L6)
- 并发 runtime 行为、具体 Python/runtime/framework 版本及部署资源预算。

特别地，HTTP 已被声明为 transport，因此上述 HTTP controls 应归入 `runtime-unknown`，不能错误标为 `not applicable`；同样，也不能仅因 source slice 没展示它们就认定实现存在漏洞。

### Not applicable

- MCP Apps、UI resource、widget、DOM escaping、`postMessage`、CSP、host rendering：项目明确不返回 UI resource，也不声明 MCP Apps compatibility。[PROJECT.md](../fixtures/already-good-2026-07-28/PROJECT.md#L7)
- stdio framing 与 parent-process stdio ownership：只声明 Streamable HTTP。
- MRTR/input retry：当前 lookup capability 不请求额外输入。
- `CacheableResult` 的 `ttlMs` / `cacheScope`：当前路径是 `tools/call`，不是受该要求覆盖的 list/read result。
- tasks extension、`subscriptions/listen`、GET/SSE resumability、legacy initialize/session compatibility：均未声明或暴露。

最终 judgment：在当前 evidence ceiling 内，这个 synthetic fixture 的 capability slice 与其“集中 validation、effect 前 gating、共享 authority、同步状态 mutation”的设计描述一致；**finding 数量为 0**。剩余项应保持 `runtime-unknown`，只有获得真实 runnable transport 或 deployment receipts 后才可继续提升或否定相应 claim。未修改文件，未进行外部披露。
